service cloud.firestore {
  match /databases/{database}/documents {
    // อนุญาตให้เข้าถึงทุกเอกสารและคอลเลกชันย่อย
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}